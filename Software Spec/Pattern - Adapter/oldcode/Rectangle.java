package adapter.oldcode;

public class Rectangle {
	public void draw(int x1, int y1, int w, int h) {
		String s = String.format("Rectangle start at %d, %d. Width = %d. Height = %d", x1, y1, w, h);
	}
}
